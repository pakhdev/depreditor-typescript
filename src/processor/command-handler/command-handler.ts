import ContainerProperties from '../../core/containers/interfaces/container-properties.interface.ts';

class CommandHandler {
    constructor() {}

    public execute(element: ContainerProperties): void {
        // Create element
        // Create workspace
        // Insert/decide wrap or unwrap
        // Adjust workspace selection
        // Run insert/wrap/unwrap command
        // Create transaction
        // Commit transaction
    }
}

export default CommandHandler;