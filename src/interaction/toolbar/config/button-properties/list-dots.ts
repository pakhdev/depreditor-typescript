import ToolbarButton from '../../interfaces/toolbar-button.interface.ts';
import containersConfig from '../../../../core/containers/config.ts';

const listDots: ToolbarButton = {
    icon: 'icon-set-list-dots',
    formattingContainerProperties: containersConfig.ul,
};

export default listDots;