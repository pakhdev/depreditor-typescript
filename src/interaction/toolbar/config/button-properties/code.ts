import ToolbarButton from '../../interfaces/toolbar-button.interface.ts';
import containersConfig from '../../../../core/containers/config.ts';

const code: ToolbarButton = {
    icon: 'icon-set-code',
    formattingContainerProperties: containersConfig.divCodeText,
};

export default code;