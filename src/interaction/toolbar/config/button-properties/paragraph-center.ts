import ToolbarButton from '../../interfaces/toolbar-button.interface.ts';
import containersConfig from '../../../../core/containers/config.ts';

const paragraphCenter: ToolbarButton = {
    icon: 'icon-set-paragraph-center',
    formattingContainerProperties: containersConfig.divAlignCenter,
};

export default paragraphCenter;